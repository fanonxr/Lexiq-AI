# Qdrant Dockerfile
# Extends the official Qdrant image with custom configuration
FROM qdrant/qdrant:latest

# Add any custom configuration files if needed
# COPY qdrant-config.yaml /qdrant/config/production.yaml

# Default command (can be overridden)
CMD ["./qdrant"]
